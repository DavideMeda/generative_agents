---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Demo — Gen_Agent">
  <section class="page">
    <h1>Demo</h1>
    <p>Run locally:</p>
    <pre>pip install -e ".[research]"
python scripts/test_simulation_200_epochs_plan_to_poi.py --preset fast --ticks 15</pre>
    <p>Outputs land in <code>outputs/</code> with JSON reports and optional dialogue traces.</p>
  </section>
</BaseLayout>
