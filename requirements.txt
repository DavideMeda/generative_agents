# ─────────────────────────────────────────────────────────────────────────────
# Single source of truth: pyproject.toml
#
# Do NOT install this file alone on a fresh clone — use:
#   pip install -e ".[dev]"              # local development + tests
#   pip install -e ".[postgres]"         # add PostgreSQL driver (prod/Docker)
#   pip install -e ".[dev,postgres,docs]"
#
# This file lists only core runtime dependencies (synced with pyproject.toml).
# The old Stanford stack (Django 2.2, selenium, gensim, …) was removed.
# ─────────────────────────────────────────────────────────────────────────────

openai>=1.0,<2
tiktoken>=0.5
numpy>=1.24,<3
sqlalchemy>=2.0,<3
alembic>=1.12
pydantic>=2.0,<3
python-dotenv>=1.0
httpx>=0.25
structlog>=23.2
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
